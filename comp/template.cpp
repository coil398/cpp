#include "bits/stdc++.h"
using namespace std;

#define ll long long
#define rep(i, a, b) for (int i = a; i < b; ++i)

int main() {
  std::ios::sync_with_stdio(false);
  cin.tie(nullptr);

  return 0;
}
